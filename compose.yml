services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/plow.db
      - POLL_INTERVAL=6
    restart: unless-stopped
